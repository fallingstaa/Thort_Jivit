{
  "name": "life_record_functions",
  "description": "Firebase Cloud Functions for life_record app",
  "scripts": {
    "lint": "eslint .",
    "serve": "firebase emulator:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "dependencies": {
    "axios": "^1.6.0",
    "ffmpeg-static": "^5.3.0",
    "ffprobe-static": "^3.1.0",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "life-record": "file:.."
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "eslint-config-google": "^0.14.0"
  }
}
